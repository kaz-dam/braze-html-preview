const Loader = () => {
    return (
        <span className="inline-block w-12 h-12 border-4 border-purple-700 rounded-full animate-spin border-b-transparent"></span>
    );
};

export default Loader;