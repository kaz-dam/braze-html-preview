export enum View {
    MOBILE = 450,
    TABLET = 750,
    DESKTOP = 1100
};

export type LoaderProps = {
    className?: string;
};

export type PushMockupProps = {
    messageTitle: string;
    messageBody: string;
};
